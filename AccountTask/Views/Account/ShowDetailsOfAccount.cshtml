@model AccountTask.Models.Account

@{
    ViewData["Title"] = "ShowDetailsOfAccount";
}

<h1>ShowDetailsOfAccount</h1>

<div>
    <h4>Account</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.FullName)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.FullName)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Locality)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Locality)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.ApartmentNumber)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.ApartmentNumber)
        </dd>
    </dl>
</div>
<div>
    <span class="btn-group">
        <a asp-action="UpdateAccount" asp-route-accountId="@Model.Id" class="btn btn-primary">Edit</a>

        <form asp-action="DeleteAccount" asp-route-accountId="@Model.Id">
            <button type="submit" onclick="return confirm('Вы действительно хотите удалить запись?')" class="btn btn-danger">Delete</button>
        </form>
    </span>

    <a asp-action="Index">Back to List</a>
    @*<a asp-action="Index" class="btn btn-info">Back to List</a>*@

    @*<a asp-action="Edit" asp-route-id="@Model.Id">Edit</a> |
        <a asp-action="Index">Back to List</a>*@
</div>
